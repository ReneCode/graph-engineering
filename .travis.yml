language: node_js
node_js:
  - "8"

before_script:
  - npm install -g firebase-tools

script:
  - echo "build the application"
  - yarn test
  - yarn build


deploy:
  provider: azure_web_apps
  on:
    condition: NOT $TRAVIS_BRANCH = "master"
